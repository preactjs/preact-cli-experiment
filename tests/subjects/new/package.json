{
	"name": "new",
	"version": "0.1.0",
	"license": "MIT",
	"author": {},
	"scripts": {
		"build": "preact build",
		"start": "if-env NODE_ENV=production && yarn -s serve || yarn -s watch",
		"watch": "preact watch"
	},
	"dependencies": {
		"preact": "^10.0.0-rc.1"
	},
	"devDependencies": {
		"if-env": "latest",
		"@preact/cli": "^0.0.4-alpha.0"
	}
}